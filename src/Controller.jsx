class Controller {
    setToastMethods(showToast, hideToast) {
        this.showToast = showToast;
        this.hideToast = hideToast
    }
}

const controller = new Controller();
export default controller;